<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.victor.coordinatorlayout">

    <application
        >
        <activity android:name=".CoordinatorMainActivity" android:theme="@style/BlueTheme">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".CoordinatorActionBarActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity android:name=".CoordinatorTabActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity android:name=".CollapsingActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity android:name=".UcHeadActivity"
            android:theme="@style/TranslucentTheme"
            />
        <activity android:name=".TitileActionBarActivity"
            android:theme="@style/TranslucentTheme"
            />

    </application>

</manifest>